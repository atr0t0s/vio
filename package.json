{
  "name": "vio-workspace",
  "private": true,
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "dev": "npm run dev --workspace=examples/starter",
    "build": "npm run build --workspace=packages/vio",
    "test": "npm run test --workspace=packages/vio"
  }
}
